---
// SeasonalTheme — gives a collection page its own visual identity: a shifted
// accent palette, a themed ornament band beneath the nav, and a soft background
// motif. All artwork is inline SVG (no external requests, scales cleanly) and
// every animation is disabled under prefers-reduced-motion.
//
// Themes: ramadan (crescents + fanous lanterns, emerald & gold)
//         christmas (stars + evergreen, silver-gold on deep pine)
//         sun-festival (solar burst + falcon disc, amber)
import type { SeasonalTheme } from "@/types";

interface Props {
  theme: SeasonalTheme;
}
const { theme } = Astro.props;
---

{theme === "ramadan" && (
  <div class="sth sth-ramadan" aria-hidden="true">
    {/* Hanging fanous lanterns */}
    <div class="sth-lanterns">
      {[0, 1, 2, 3, 4, 5, 6].map((i) => (
        <div class={`sth-lantern sth-l${i % 4}`}>
          <span class="sth-cord"></span>
          <svg viewBox="0 0 40 62" width="34" height="53">
            <defs>
              <linearGradient id={`lg${i}`} x1="0" y1="0" x2="0" y2="1">
                <stop offset="0%" stop-color="#F7E6AE" stop-opacity=".95" />
                <stop offset="55%" stop-color="#D9B45A" stop-opacity=".8" />
                <stop offset="100%" stop-color="#A87E32" stop-opacity=".7" />
              </linearGradient>
            </defs>
            {/* crown */}
            <path d="M20 2 L26 8 H14 Z" fill="#D9B45A" opacity=".9" />
            <rect x="12" y="8" width="16" height="3" rx="1" fill="#D9B45A" opacity=".85" />
            {/* body */}
            <path d="M11 11 H29 L33 24 L29 46 H11 L7 24 Z" fill={`url(#lg${i})`} stroke="#D9B45A" stroke-width=".8" />
            {/* glass panes */}
            <path d="M15 15 L20 20 L25 15 L25 38 L20 43 L15 38 Z" fill="#2F7E66" opacity=".45" />
            <path d="M20 20 V43" stroke="#F7E6AE" stroke-width=".6" opacity=".5" />
            {/* base + tassel */}
            <rect x="13" y="46" width="14" height="3" rx="1" fill="#D9B45A" opacity=".9" />
            <path d="M20 49 V56" stroke="#D9B45A" stroke-width="1" opacity=".8" />
            <circle cx="20" cy="58" r="2.6" fill="#D9B45A" opacity=".85" />
          </svg>
        </div>
      ))}
    </div>
    {/* Crescent + star */}
    <svg class="sth-crescent" viewBox="0 0 120 120" width="120" height="120">
      <path d="M78 18a44 44 0 1 0 26 74A36 36 0 1 1 78 18Z" fill="none" stroke="#D9B45A" stroke-width="2" opacity=".55" />
      <path d="M96 30l3.2 7.4 8 .8-6 5.4 1.7 7.9-6.9-4.2-6.9 4.2 1.7-7.9-6-5.4 8-.8Z" fill="#F7E6AE" opacity=".6" />
    </svg>
  </div>
)}

{theme === "christmas" && (
  <div class="sth sth-christmas" aria-hidden="true">
    {/* Starfield */}
    <div class="sth-stars">
      {[...Array(22)].map((_, i) => (
        <span class={`sth-star sth-s${i % 5}`} style={`left:${(i * 4.5 + (i % 3) * 2.2) % 98}%;top:${(i * 7 + (i % 4) * 9) % 88}%`}></span>
      ))}
    </div>
    {/* Ornament band: a garland swag hung with baubles, crowned by a star */}
    <div class="sth-band">
      <svg viewBox="0 0 460 92" width="460" height="92">
        <defs>
          <linearGradient id="xgar" x1="0" y1="0" x2="1" y2="0">
            <stop offset="0%" stop-color="#D9B45A" stop-opacity="0" />
            <stop offset="22%" stop-color="#D9B45A" stop-opacity=".75" />
            <stop offset="50%" stop-color="#FFF6DA" stop-opacity=".9" />
            <stop offset="78%" stop-color="#D9B45A" stop-opacity=".75" />
            <stop offset="100%" stop-color="#D9B45A" stop-opacity="0" />
          </linearGradient>
        </defs>
        {/* garland swag */}
        <path d="M10 30 Q230 76 450 30" fill="none" stroke="url(#xgar)" stroke-width="1.4" />
        {/* hanging baubles */}
        <g opacity=".9">
          <path d="M118 46v9" stroke="#D9B45A" stroke-width="1" opacity=".65" />
          <circle cx="118" cy="60" r="5.5" fill="none" stroke="#D9B45A" stroke-width="1.3" />
          <circle cx="118" cy="58" r="1.6" fill="#FFF6DA" opacity=".7" />
          <path d="M342 46v9" stroke="#D9B45A" stroke-width="1" opacity=".65" />
          <circle cx="342" cy="60" r="5.5" fill="none" stroke="#D9B45A" stroke-width="1.3" />
          <circle cx="342" cy="58" r="1.6" fill="#FFF6DA" opacity=".7" />
          <path d="M230 58v10" stroke="#D9B45A" stroke-width="1" opacity=".55" />
          <circle cx="230" cy="74" r="4" fill="none" stroke="#2F7E66" stroke-width="1.3" opacity=".9" />
        </g>
        {/* crowning eight-point star */}
        <path d="M230 2 L236 22 L256 28 L236 34 L230 54 L224 34 L204 28 L224 22 Z"
              fill="#FFF6DA" opacity=".9" />
        <path d="M230 14 L232.6 25.4 L244 28 L232.6 30.6 L230 42 L227.4 30.6 L216 28 L227.4 25.4 Z"
              fill="#fff" opacity=".65" />
        {/* small flanking stars */}
        <path d="M158 26l2 6 6 2-6 2-2 6-2-6-6-2 6-2Z" fill="#D9B45A" opacity=".7" />
        <path d="M302 26l2 6 6 2-6 2-2 6-2-6-6-2 6-2Z" fill="#D9B45A" opacity=".7" />
      </svg>
    </div>
  </div>
)}

{theme === "spring" && (
  <div class="sth sth-spring" aria-hidden="true">
    {/* Drifting blossom petals */}
    <div class="sth-petals">
      {[...Array(14)].map((_, i) => (
        <span class={`sth-petal sth-p${i % 5}`} style={`left:${(i * 7.3 + (i % 3) * 3) % 96}%`}></span>
      ))}
    </div>
    {/* Garland of painted eggs + spring shoots */}
    <div class="sth-band sth-spring-band">
      <svg viewBox="0 0 420 84" width="420" height="84">
        <defs>
          <linearGradient id="spgar" x1="0" y1="0" x2="1" y2="0">
            <stop offset="0%" stop-color="#7FB086" stop-opacity="0" />
            <stop offset="30%" stop-color="#7FB086" stop-opacity=".7" />
            <stop offset="70%" stop-color="#7FB086" stop-opacity=".7" />
            <stop offset="100%" stop-color="#7FB086" stop-opacity="0" />
          </linearGradient>
        </defs>
        <path d="M14 26 Q210 66 406 26" fill="none" stroke="url(#spgar)" stroke-width="1.4" />
        {/* painted eggs hanging from the vine */}
        {[
          { x: 96, y: 50, c: "#D9B45A" },
          { x: 168, y: 58, c: "#C06B3A" },
          { x: 252, y: 58, c: "#4385A0" },
          { x: 324, y: 50, c: "#9A4456" },
        ].map((e) => (
          <g opacity=".9">
            <path d={`M${e.x} ${e.y - 12}v6`} stroke="#7FB086" stroke-width="1" opacity=".7" />
            <ellipse cx={e.x} cy={e.y + 4} rx="7" ry="9.5" fill="none" stroke={e.c} stroke-width="1.5" />
            <path d={`M${e.x - 6} ${e.y + 2}q6 3 12 0 M${e.x - 6} ${e.y + 7}q6 3 12 0`} stroke={e.c} stroke-width="1" fill="none" opacity=".75" />
          </g>
        ))}
        {/* young shoots */}
        <g stroke="#7FB086" stroke-width="1.5" fill="none" opacity=".85" stroke-linecap="round">
          <path d="M210 24v-16M210 14q-8-4-10-11M210 16q8-5 11-12" />
        </g>
        <circle cx="210" cy="6" r="2.6" fill="#F7E6AE" opacity=".85" />
      </svg>
    </div>
  </div>
)}

{theme === "red-sea" && (
  <div class="sth sth-redsea" aria-hidden="true">
    {/* Light shafts through water + rolling swell */}
    <div class="sth-shafts"></div>
    <svg class="sth-swell" viewBox="0 0 1200 120" width="1200" height="120" preserveAspectRatio="none">
      <path d="M0 74 Q150 44 300 74 T600 74 T900 74 T1200 74 V120 H0Z" fill="#2C8E86" opacity=".14" />
      <path d="M0 88 Q150 62 300 88 T600 88 T900 88 T1200 88 V120 H0Z" fill="#4385A0" opacity=".12" />
      <path d="M0 74 Q150 44 300 74 T600 74 T900 74 T1200 74" fill="none" stroke="#7FD8CE" stroke-width="1" opacity=".3" />
    </svg>
    {/* Rising bubbles */}
    <div class="sth-bubbles">
      {[...Array(10)].map((_, i) => (
        <span class={`sth-bubble sth-b${i % 4}`} style={`left:${(i * 9.5 + (i % 3) * 4) % 94}%`}></span>
      ))}
    </div>
  </div>
)}

{theme === "honeymoon" && (
  <div class="sth sth-honeymoon" aria-hidden="true">
    {/* Soft rose-gold glow + a delicate lotus pair */}
    <div class="sth-glow"></div>
    {/* A lotus in bloom on two entwined stems — the Egyptian emblem of union */}
    <svg class="sth-lotus" viewBox="0 0 200 96" width="200" height="96">
      <g stroke="#E8B4A0" stroke-width="1.5" fill="none" opacity=".9" stroke-linejoin="round">
        {/* outer petals */}
        <path d="M100 54 C 82 52, 66 46, 54 36 C 70 34, 88 42, 100 54 Z" />
        <path d="M100 54 C 118 52, 134 46, 146 36 C 130 34, 112 42, 100 54 Z" />
        {/* inner petals */}
        <path d="M100 54 C 88 44, 80 32, 76 20 C 88 26, 96 40, 100 54 Z" />
        <path d="M100 54 C 112 44, 120 32, 124 20 C 112 26, 104 40, 100 54 Z" />
        {/* central petal */}
        <path d="M100 54 C 94 40, 94 24, 100 10 C 106 24, 106 40, 100 54 Z" />
        {/* entwined stems */}
        <path d="M100 56 C 96 68, 84 76, 72 82" />
        <path d="M100 56 C 104 68, 116 76, 128 82" />
      </g>
      <circle cx="100" cy="34" r="2.2" fill="#F7E6AE" opacity=".85" />
      <path d="M100 86l1.6 4.4 4.4 1.6-4.4 1.6-1.6 4.4-1.6-4.4-4.4-1.6 4.4-1.6Z" fill="#F7E6AE" opacity=".6" />
    </svg>
  </div>
)}

{theme === "eid" && (
  <div class="sth sth-eid" aria-hidden="true">
    {/* Festive bunting — two swags of pennant flags */}
    <svg class="sth-bunting" viewBox="0 0 460 70" width="460" height="70">
      <path d="M8 12 Q230 52 452 12" fill="none" stroke="#D9B45A" stroke-width="1.2" opacity=".7" />
      {[36,96,156,216,276,336,396].map((x, i) => {
        const y = 12 + Math.sin(((x - 8) / 444) * Math.PI) * 38;
        const c = ["#2F7E66", "#D9B45A", "#9A4456", "#4385A0"][i % 4];
        return <path d={`M${x - 9} ${y} L${x + 9} ${y} L${x} ${y + 17} Z`} fill={c} opacity=".8" />;
      })}
    </svg>
    <svg class="sth-eid-moon" viewBox="0 0 90 90" width="86" height="86">
      <path d="M60 12a34 34 0 1 0 20 58A28 28 0 1 1 60 12Z" fill="none" stroke="#D9B45A" stroke-width="2" opacity=".6" />
      <path d="M72 24l2.4 5.6 6 .6-4.5 4 1.3 6-5.2-3.2-5.2 3.2 1.3-6-4.5-4 6-.6Z" fill="#F7E6AE" opacity=".7" />
    </svg>
  </div>
)}

{theme === "sun-festival" && (
  <div class="sth sth-sun" aria-hidden="true">
    {/* Radiating solar burst */}
    <svg class="sth-rays" viewBox="0 0 400 200" width="400" height="200" preserveAspectRatio="xMidYMin slice">
      <defs>
        <radialGradient id="sunGlow" cx="50%" cy="0%" r="70%">
          <stop offset="0%" stop-color="#F7E6AE" stop-opacity=".38" />
          <stop offset="55%" stop-color="#D9822B" stop-opacity=".12" />
          <stop offset="100%" stop-color="#D9822B" stop-opacity="0" />
        </radialGradient>
      </defs>
      <rect width="400" height="200" fill="url(#sunGlow)" />
      <g stroke="#E8A33D" stroke-width="1" opacity=".3">
        {[...Array(19)].map((_, i) => {
          const a = (Math.PI * i) / 18;
          return <line x1="200" y1="6" x2={200 + Math.cos(a) * -220} y2={6 + Math.sin(a) * 220} />;
        })}
      </g>
      {/* winged solar disc */}
      <circle cx="200" cy="16" r="11" fill="#E8A33D" opacity=".85" />
      <path d="M189 16q-26 -9 -46 -2 22 3 46 6Z M211 16q26 -9 46 -2 -22 3 -46 6Z" fill="#D9B45A" opacity=".65" />
    </svg>
  </div>
)}

<style is:global>
  /* ---------------------------------------------------------------------
     Shared theme scaffolding
     --------------------------------------------------------------------- */
  .sth { position: absolute; inset: 0; pointer-events: none; overflow: hidden; z-index: 1; }

  /* Themed pages get a scoped accent + ground shift. The page article carries
     .themed-<name>; everything reads through the existing custom properties,
     so components need no changes. */
  .themed-ramadan {
    --gold: #E0BE6A; --gold-bright: #FBEFC4; --gold-deep: #9C7A2E;
    --line: rgba(47,126,102,.26); --line-2: rgba(224,190,106,.44);
  }
  .themed-ramadan .phero {
    background:
      radial-gradient(760px 400px at 50% 0%, rgba(47,126,102,.20), transparent 62%),
      radial-gradient(600px 320px at 50% 6%, rgba(247,230,174,.12), transparent 60%);
  }
  .themed-christmas {
    --gold: #E3C579; --gold-bright: #FFF6DA; --gold-deep: #A8823A;
    --line: rgba(255,255,255,.16); --line-2: rgba(227,197,121,.46);
  }
  .themed-christmas .phero {
    background:
      radial-gradient(780px 420px at 50% 0%, rgba(47,126,102,.16), transparent 60%),
      radial-gradient(560px 300px at 50% 2%, rgba(255,246,218,.16), transparent 62%);
  }
  .themed-sun-festival {
    --gold: #E8A33D; --gold-bright: #FBE2A6; --gold-deep: #A9631C;
    --line: rgba(232,163,61,.24); --line-2: rgba(232,163,61,.46);
  }
  .themed-sun-festival .phero {
    background: radial-gradient(820px 440px at 50% 0%, rgba(232,163,61,.20), transparent 62%);
  }
  .themed-eid {
    --gold: #E0BE6A; --gold-bright: #FBEFC4; --gold-deep: #9C7A2E;
    --line: rgba(47,126,102,.24); --line-2: rgba(224,190,106,.44);
  }
  .themed-eid .phero {
    padding-top: 196px; position: relative;
    background:
      radial-gradient(740px 380px at 50% 0%, rgba(47,126,102,.16), transparent 62%),
      radial-gradient(560px 300px at 50% 4%, rgba(251,239,196,.12), transparent 60%);
  }
  .themed-eid .phero > .wrap { position: relative; z-index: 2; }
  .sth-bunting { position: absolute; left: 50%; top: 96px; transform: translateX(-50%); opacity: .85; }
  .sth-eid-moon { position: absolute; right: 6%; top: 44%; opacity: .55;
    filter: drop-shadow(0 0 18px rgba(217,180,90,.3)); }
  @media(max-width:600px){ .sth-bunting{ transform:translateX(-50%) scale(.8); top:86px; } .sth-eid-moon{ display:none; } }

  /* Make the page hero the positioning context for the ornament layer, and keep
     the heading/breadcrumb above it. */
  .themed-ramadan .phero,
  .themed-christmas .phero,
  .themed-sun-festival .phero { position: relative; }
  .themed-ramadan .phero > .wrap,
  .themed-christmas .phero > .wrap,
  .themed-sun-festival .phero > .wrap { position: relative; z-index: 2; }
  /* Give the ornament room to breathe above the heading. */
  .themed-ramadan .phero { padding-top: 214px; }
  .themed-sun-festival .phero { padding-top: 206px; }
  .themed-christmas .phero { padding-top: 200px; }

  /* ---------------------------------------------------------------------
     Ramadan — hanging fanous lanterns + crescent
     --------------------------------------------------------------------- */
  /* Hang below the fixed nav (86px) so lanterns never cross the menu. */
  .sth-lanterns { position: absolute; top: 94px; left: 0; right: 0; display: flex;
    justify-content: space-around; padding: 0 4%; }
  .sth-lantern { position: relative; display: flex; flex-direction: column; align-items: center;
    transform-origin: top center; animation: sth-sway 7s ease-in-out infinite; opacity: .85;
    filter: drop-shadow(0 6px 18px rgba(217,180,90,.28)); }
  .sth-cord { display: block; width: 1px; background: linear-gradient(180deg, rgba(217,180,90,.55), rgba(217,180,90,.15)); }
  .sth-l0 { animation-delay: 0s; }      .sth-l0 .sth-cord { height: 46px; }
  .sth-l1 { animation-delay: -1.6s; }   .sth-l1 .sth-cord { height: 82px; }
  .sth-l2 { animation-delay: -3.1s; }   .sth-l2 .sth-cord { height: 30px; }
  .sth-l3 { animation-delay: -4.7s; }   .sth-l3 .sth-cord { height: 64px; }
  @keyframes sth-sway { 0%,100% { transform: rotate(-2.2deg); } 50% { transform: rotate(2.2deg); } }
  .sth-crescent { position: absolute; right: 5%; top: 46%; opacity: .5;
    filter: drop-shadow(0 0 22px rgba(217,180,90,.3)); }
  @media(max-width:900px){ .sth-lantern:nth-child(n+5){ display:none; } .sth-crescent{ width:78px;height:78px;right:3%;top:52%; } }
  @media(max-width:600px){ .sth-lantern:nth-child(n+4){ display:none; } .sth-crescent{ display:none; } }

  /* ---------------------------------------------------------------------
     Christmas & New Year — starfield + ornament band
     --------------------------------------------------------------------- */
  .sth-stars { position: absolute; inset: 0; }
  .sth-star { position: absolute; width: 3px; height: 3px; border-radius: 50%;
    background: #FFF6DA; opacity: .5; box-shadow: 0 0 8px rgba(255,246,218,.75);
    animation: sth-twinkle 4.5s ease-in-out infinite; }
  .sth-s0 { animation-delay: 0s; }    .sth-s1 { animation-delay: -.9s; width:2px;height:2px; }
  .sth-s2 { animation-delay: -1.8s; } .sth-s3 { animation-delay: -2.7s; width:4px;height:4px; }
  .sth-s4 { animation-delay: -3.6s; width:2px;height:2px; }
  @keyframes sth-twinkle { 0%,100% { opacity: .18; transform: scale(.85); } 50% { opacity: .85; transform: scale(1.15); } }
  .sth-band { position: absolute; left: 50%; top: 104px; transform: translateX(-50%); opacity: .8; }
  @media(max-width:600px){ .sth-band{ transform:translateX(-50%) scale(.78); top:88px; } }

  /* ---------------------------------------------------------------------
     Abu Simbel Sun Festival — solar burst
     --------------------------------------------------------------------- */
  .sth-rays { position: absolute; top: 88px; left: 50%; transform: translateX(-50%);
    width: min(1100px, 130%); height: auto; opacity: .9; }
  .themed-sun-festival .phero h1 { text-shadow: 0 0 42px rgba(232,163,61,.22); }

  /* ---------------------------------------------------------------------
     Spring / Sham El-Nessim — drifting petals + painted-egg garland
     --------------------------------------------------------------------- */
  .themed-spring {
    --gold: #DCC06B; --gold-bright: #FBF3D2; --gold-deep: #9C8438;
    --line: rgba(127,176,134,.26); --line-2: rgba(220,192,107,.44);
  }
  .themed-spring .phero {
    padding-top: 200px;
    background:
      radial-gradient(760px 400px at 50% 0%, rgba(127,176,134,.20), transparent 62%),
      radial-gradient(560px 300px at 50% 4%, rgba(251,243,210,.12), transparent 60%);
  }
  .sth-petals { position: absolute; inset: 0; }
  .sth-petal { position: absolute; top: -14px; width: 9px; height: 6px;
    border-radius: 60% 40% 55% 45%; background: linear-gradient(135deg,#F6D9D2,#E8B4A0);
    opacity: .5; animation: sth-fall 13s linear infinite; }
  .sth-p0 { animation-delay: 0s; }    .sth-p1 { animation-delay: -2.6s; width:7px;height:5px; }
  .sth-p2 { animation-delay: -5.2s; } .sth-p3 { animation-delay: -7.8s; width:11px;height:7px; }
  .sth-p4 { animation-delay: -10.4s; }
  @keyframes sth-fall {
    0%   { transform: translateY(0) rotate(0deg); opacity: 0; }
    12%  { opacity: .55; }
    100% { transform: translateY(300px) translateX(46px) rotate(300deg); opacity: 0; }
  }
  .sth-spring-band { top: 100px; }

  /* ---------------------------------------------------------------------
     Red Sea — light shafts, swell and rising bubbles
     --------------------------------------------------------------------- */
  .themed-red-sea {
    --gold: #6FD0C6; --gold-bright: #CFF3EE; --gold-deep: #2C8E86;
    --line: rgba(44,142,134,.28); --line-2: rgba(111,208,198,.44);
  }
  .themed-red-sea .phero {
    padding-top: 196px;
    background:
      linear-gradient(180deg, rgba(44,142,134,.16), transparent 62%),
      radial-gradient(760px 380px at 50% 0%, rgba(67,133,160,.20), transparent 64%);
  }
  /* Soft caustic light shafts — wide, feathered and skewed so they read as
     sunlight through water rather than hard bands. */
  .sth-shafts { position: absolute; top: 60px; left: -10%; right: -10%; height: 330px;
    transform: skewX(-11deg); filter: blur(22px); opacity: .85;
    background:
      linear-gradient(90deg, transparent 12%, rgba(207,243,238,.075) 20%, transparent 29%),
      linear-gradient(90deg, transparent 40%, rgba(207,243,238,.055) 50%, transparent 60%),
      linear-gradient(90deg, transparent 70%, rgba(207,243,238,.065) 79%, transparent 88%);
    -webkit-mask-image: linear-gradient(180deg, rgba(0,0,0,.95), transparent 88%);
    mask-image: linear-gradient(180deg, rgba(0,0,0,.95), transparent 88%);
  }
  .sth-swell { position: absolute; left: 0; right: 0; bottom: -1px; width: 100%; height: 120px; }
  .sth-bubbles { position: absolute; inset: 0; }
  .sth-bubble { position: absolute; bottom: -12px; width: 6px; height: 6px; border-radius: 50%;
    border: 1px solid rgba(207,243,238,.55); animation: sth-rise 11s linear infinite; opacity: 0; }
  .sth-b0 { animation-delay: 0s; }    .sth-b1 { animation-delay: -2.8s; width:4px;height:4px; }
  .sth-b2 { animation-delay: -5.5s; } .sth-b3 { animation-delay: -8.2s; width:8px;height:8px; }
  @keyframes sth-rise {
    0%   { transform: translateY(0) translateX(0); opacity: 0; }
    14%  { opacity: .5; }
    100% { transform: translateY(-330px) translateX(24px); opacity: 0; }
  }

  /* ---------------------------------------------------------------------
     Honeymoon — soft rose-gold glow + entwined lotus
     --------------------------------------------------------------------- */
  .themed-honeymoon {
    --gold: #E3B98C; --gold-bright: #FBE7D6; --gold-deep: #A9764B;
    --line: rgba(232,180,160,.24); --line-2: rgba(227,185,140,.46);
  }
  .themed-honeymoon .phero {
    padding-top: 198px;
    background:
      radial-gradient(780px 420px at 50% 0%, rgba(232,180,160,.20), transparent 62%),
      radial-gradient(520px 280px at 50% 6%, rgba(251,231,214,.12), transparent 60%);
  }
  .sth-glow { position: absolute; top: 60px; left: 50%; transform: translateX(-50%);
    width: min(760px, 88%); height: 260px; border-radius: 50%;
    background: radial-gradient(closest-side, rgba(232,180,160,.20), transparent 72%);
    filter: blur(8px); }
  .sth-lotus { position: absolute; top: 96px; left: 50%; transform: translateX(-50%); opacity: .9;
    filter: drop-shadow(0 6px 20px rgba(232,180,160,.28)); }
  @media(max-width:600px){ .sth-lotus{ transform:translateX(-50%) scale(.78); top:88px; } }

  @media (prefers-reduced-motion: reduce) {
    .sth-lantern, .sth-star, .sth-petal, .sth-bubble { animation: none !important; }
    .sth-petal, .sth-bubble { display: none; }
  }
</style>
