---
// OurProcess — how a Kemet journey comes together, step by step.
// Steps are authored in company.process — not hardcoded here.
// Reusable on about page, tour pages, homepage, etc.
import { company } from "@/data/company";

interface Props {
  heading?: string;
  eyebrow?: string;
}
const {
  heading = "How it works",
  eyebrow = "Our process",
} = Astro.props;
const steps = company.process ?? [];
---
{steps.length > 0 && (
  <div>
    <div class="eyebrow" style="margin-bottom:var(--s-1)">{eyebrow}</div>
    <h2 style="margin-bottom:var(--s-5)">{heading}</h2>
    <div class="grid-3" style="gap:var(--s-4)">
      {steps.map((step) => (
        <div class="tblock reveal">
          <div class="eyebrow" style="margin-bottom:var(--s-1)">Step {step.step}</div>
          <h3>{step.heading}</h3>
          <p>{step.body}</p>
        </div>
      ))}
    </div>
  </div>
)}
